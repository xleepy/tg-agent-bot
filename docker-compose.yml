services:
  data:
    image: alpine:3.20
    container_name: tg-agent-bot-data
    restart: unless-stopped
    command: ["sh", "-c", "mkdir -p /data && tail -f /dev/null"]
    volumes:
      - ./data:/data
